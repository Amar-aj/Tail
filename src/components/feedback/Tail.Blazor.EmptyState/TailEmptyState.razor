@inherits TailComponentBase
@using Microsoft.AspNetCore.Components.Web
@using Tail.Blazor.EmptyState

<div class="@GetEmptyStateClasses()" style="@GetEmptyStateStyles()">
    @if (Icon != null)
    {
        <div class="mb-4">@Icon</div>
    }
    else if (ShowDefaultIcon)
    {
        <div class="mb-4 text-6xl opacity-50">📭</div>
    }
    @if (!string.IsNullOrEmpty(Title))
    {
        <h3 class="text-xl font-semibold mb-2" style="color: var(--color-text-primary);">@Title</h3>
    }
    @if (!string.IsNullOrEmpty(Description))
    {
        <p class="mb-4" style="color: var(--color-text-secondary);">@Description</p>
    }
    @if (Action != null)
    {
        <div>@Action</div>
    }
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public RenderFragment? Icon { get; set; }
    [Parameter] public RenderFragment? Action { get; set; }
    [Parameter] public string? Title { get; set; }
    [Parameter] public string? Description { get; set; }
    [Parameter] public EmptyStateSize Size { get; set; } = EmptyStateSize.Md;
    [Parameter] public EmptyStateVariant Variant { get; set; } = EmptyStateVariant.Default;
    [Parameter] public bool ShowDefaultIcon { get; set; } = true;
    [Parameter] public string? Style { get; set; }

    private string GetEmptyStateClasses()
    {
        var classes = new List<string> { "flex", "flex-col", "items-center", "justify-center", "text-center", "p-8" };

        classes.Add(Size switch
        {
            EmptyStateSize.Sm => "p-4",
            EmptyStateSize.Md => "p-8",
            EmptyStateSize.Lg => "p-12",
            _ => "p-8"
        });

        if (!string.IsNullOrEmpty(Class))
        {
            classes.Add(Class);
        }

        return string.Join(" ", classes);
    }

    private string GetEmptyStateStyles()
    {
        var styles = new List<string>();
        styles.Add($"background-color: var(--color-surface, white)");
        if (!string.IsNullOrEmpty(Style))
        {
            styles.Add(Style);
        }
        return string.Join("; ", styles);
    }
}


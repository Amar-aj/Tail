@page "/components/forms/passwordstrengthmeter"
@using Tail.Blazor.Docs.Shared
@using Microsoft.AspNetCore.Components
@using Tail.Blazor.PasswordStrengthMeter

<PageTitle>PasswordStrengthMeter - Tail.Blazor</PageTitle>

<DocPageTemplate Title="PasswordStrengthMeter" 
                 Description="Tail.Blazor.PasswordStrengthMeter"
                 PackageName="Tail.Blazor.PasswordStrengthMeter"
                 ApiParameters="@apiParameters">
    
    <DocSection Title="Installation">
        <CodePreview Title="Install Package" Code="@installCode" ShowPreview="false" />
    </DocSection>

    <DocSection Title="Basic Usage">
        <p class="mb-4" style="color: var(--color-text-secondary);">
            Represents a passwordstrengthmeter component with material design theme and comes with multiple functions.
        </p>
        <CodePreview Title="Basic PasswordStrengthMeter" Code="@basicCode">
            <PreviewContent>
                <div class="p-4">
                    @if (isGeneric)
                    {
                        <p class="text-sm" style="color: var(--color-text-secondary);">See code examples for proper usage with type parameters.</p>
                    }
                    else
                    {
                        <TailPasswordStrengthMeter Placeholder="Enter text..." />
                    }
                </div>
            </PreviewContent>
                </CodePreview>
    </DocSection>

    <DocSection Title="Features">
        <div style="color: var(--color-text-primary);">
            <ul class="space-y-2" style="list-style: disc; padding-left: 1.5rem;">
                <li>Full theme support with CSS variables</li>
                <li>Responsive design for all screen sizes</li>
                <li>Smooth transitions and animations</li>
                <li>Accessibility features built-in</li>
                <li>Lightweight and performant</li>
                <li>Type-safe with IntelliSense support</li>
            </ul>
        </div>
    </DocSection>
</DocPageTemplate>

@code {
    private bool isGeneric = false;

    private string installCode = @"dotnet add package Tail.Blazor.PasswordStrengthMeter";

    private string basicCode = @"<TailPasswordStrengthMeter Placeholder=""Enter text..."" />";

    private List<DocPageTemplate.ApiParameter> apiParameters = new()
    {
    };
}
